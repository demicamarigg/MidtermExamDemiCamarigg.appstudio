/*

//making the function
function calcAvgSquare(num1, num2, num3){
  let avgSquare = ((num1 + num2 + num3) / 3 ) * (num1**2)
  return(avgSquare)
  }
  
  
  let n1 = Number(prompt("Enter one number"))
  let n2 = Number(prompt("Enter another number"))
  let n3 = Number(prompt("Enter one last number"))
  
  let avgSquareAnswer = calcAvgSquare(n1, n2, n3)
  
  alert(`The answer is ${avgSquareAnswer}`)
  */